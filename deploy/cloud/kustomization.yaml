apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../k8s

patches:
  - target:
      kind: Ingress
      name: lipid-classifier
    patch: |-
      - op: remove
        path: /spec/rules/0/host
  - path: secrets.cloud.yaml
